language: c

dist: trusty
sudo: required

services:

compiler:
  - gcc

install:
  - ./install-ghdl.sh
  - source setenv.sh
  - ghdl --version

script:
  - source setenv.sh
  - make bios
  - make -C vhdl all
  - make -C vhdl test


after_success:
